{
  "name": "tpi-submit-bot",
  "version": "1.0.0",
  "description": "A bot to submit data to TPI Suitcase, triggered by an API.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "echo \"No build step required for Node.js\"",
    "docker:build": "docker build -t tpi-submit-bot .",
    "docker:run": "docker run -p 3001:3001 --env-file .env tpi-submit-bot",
    "docker:compose": "docker-compose up --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "api",
    "bot",
    "playwright",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.10.0",
    "dotenv": "^17.2.0",
    "express": "^4.19.2",
    "playwright": "^1.55.0",
    "uuid": "^9.0.1"
  }
}
